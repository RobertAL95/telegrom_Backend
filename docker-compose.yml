version: '3.8'

services:
  backend:
    build: .
    container_name: backend_app
    ports:
      - "5001:4000"   # host:container -> accesible en Windows por http://localhost:5001
    environment:
      - PORT=4000
      - MONGO_URI=mongodb://mongo:27017/mydb
      - GOOGLE_CLIENT_ID=TU_CLIENT_ID_REAL
      - GOOGLE_CLIENT_SECRET=TU_CLIENT_SECRET_REAL
      - GOOGLE_CALLBACK_URL=http://localhost:5001/auth/google/callback
      - SESSION_SECRET=secret123
      - JWT_SECRET=secret123
      - FRONTEND_URL=http://localhost:3000
    depends_on:
      - mongo
    networks:
      - appnet

  mongo:
    image: mongo:6
    container_name: mongodb
    restart: always
    ports:
      - "27017:27017"  # accesible desde tu host en localhost:27017
    volumes:
      - mongo_data:/data/db
    networks:
      - appnet

  mongo-express:
    image: mongo-express:latest
    container_name: mongo_express
    restart: always
    ports:
      - "8081:8081"   # interfaz gráfica en http://localhost:8081
    environment:
      - ME_CONFIG_MONGODB_SERVER=mongo
      - ME_CONFIG_MONGODB_PORT=27017
      - ME_CONFIG_BASICAUTH_USERNAME=admin
      - ME_CONFIG_BASICAUTH_PASSWORD=admin
    depends_on:
      - mongo
    networks:
      - appnet

volumes:
  mongo_data:

networks:
  appnet:
    driver: bridge
